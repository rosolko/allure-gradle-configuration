plugins {
    id "java-library"
    id "io.qameta.allure" version "2.9.6"
    id "com.github.ben-manes.versions" version "0.39.0"
}

group "com.github.rosolko"
version "1.0.0"

java {
    toolchain {
        languageVersion = JavaLanguageVersion.of(8)
    }
}

allure {
    version = "2.14.0"
}

repositories {
    mavenCentral()
}

dependencies {
    implementation("com.squareup.retrofit2:converter-gson:2.9.0")
    implementation("io.qameta.allure:allure-okhttp3:2.17.0")
    testImplementation("com.codeborne:selenide:6.1.1")
    testImplementation("org.junit.jupiter:junit-jupiter:5.8.2")
    testImplementation("io.qameta.allure:allure-selenide:2.17.0")
    testImplementation("io.undertow:undertow-core:2.2.14.Final")
    testRuntimeOnly("io.qameta.allure:allure-junit5:2.17.1")
    testRuntimeOnly("org.slf4j:slf4j-simple:1.7.32")
}

tasks.withType(Test) {
    useJUnitPlatform()
    testLogging.showStandardStreams = true
    maxParallelForks = (int) (Runtime.runtime.availableProcessors().intdiv(2) ?: 1)
}

wrapper.gradleVersion = "6.7"
